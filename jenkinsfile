pipeline {
    agent any

    parameters {
        string(name: 'NAME', defaultValue: 'Moin', description: 'Enter your name')
    }

    stages {
        stage('Printing Parameters') {
            steps {
                echo "Hello, ${params.NAME}"
            }
        }
    }
}
